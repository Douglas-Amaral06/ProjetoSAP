# Dependências principais
streamlit>=1.35
pandas>=2.0
python-dotenv>=1.0
langchain-core>=0.2
langchain-google-genai>=1.0
werkzeug>=2.3

# Busca semântica (R3)
sentence-transformers>=2.2
numpy>=1.24

# Auto-refresh (opcional)
streamlit-autorefresh>=1.0

# PostgreSQL (R9)
psycopg2-binary>=2.9

# Para treinamento ML (R11 - opcional)
torch>=2.0
transformers>=4.30
scikit-learn>=1.3
mlflow>=2.0
jupyter>=1.0

# Para desenvolvimento/testes
pytest>=7.0
black>=23.0
flake8>=6.0

# Para migração SQLite->PostgreSQL (R9)
pgloader>=3.6  # Instalar separadamente (sistema)

# Para anexos (R10) - já incluso no Streamlit
# st.file_uploader e st.download_button nativos